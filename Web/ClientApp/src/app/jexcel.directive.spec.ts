import { JexcelDirective } from './jexcel.directive';

describe('JexcelDirective', () => {
  it('should create an instance', () => {
    const directive = new JexcelDirective();
    expect(directive).toBeTruthy();
  });
});
